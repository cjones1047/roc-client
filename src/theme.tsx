import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#1976d2', // You can customize this
        },
        secondary: {
            main: '#ff9800', // And this too
        },
    },
});

export default theme;